[uwsgi]
module = DjangoServer.wsgi:application

socket = :8000

master = true
processes = 4

chdir = /app/DjangoServer  # 이 경로가 실제 프로젝트 루트 디렉토리를 가리킵니다.

vacuum = true

touch-reload = /app/DjangoServer/wsgi.py  # 프로젝트 루트 내의 wsgi.py 파일을 가리킵니다.

# 로그 파일 경로
logto = /app/logs/uwsgi.log
